version: 2
jobs:
  build:
    docker:
      - image: circleci/buildpack-deps:18.04-browsers

    steps:
      - checkout

      - run:
          name: test
          command: make check

      - run:
          name: build
          command: make